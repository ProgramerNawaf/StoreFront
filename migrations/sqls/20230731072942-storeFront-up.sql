/* Replace with your SQL commands */CREATE TABLE product (
    id SERIAL PRIMARY  KEY,
    name VARCHAR(150),
    price VARCHAR(150)

);
CREATE TABLE myuser (
    id SERIAL PRIMARY  KEY,
    username VARCHAR(150),
    password VARCHAR(150)

);